apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: rssie-pool
  namespace: metallb-system
spec:
  addresses:
  - 172.21.0.10-172.21.0.30 # Adjust this to match your docker network (kind network)
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: rssie-advertisement
  namespace: metallb-system
